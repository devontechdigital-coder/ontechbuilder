export default function PublicHomePage() {
  return (
    <main className="shell">
      <section className="status">
        <p className="eyebrow">Public renderer foundation</p>
        <h1>Published site renderer is running.</h1>
        <p>
          This placeholder proves the rendering app boots. Published content rendering will be
          implemented in a later milestone.
        </p>
      </section>
    </main>
  );
}
